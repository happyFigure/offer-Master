[project]
name = "jobpilot-worker"
version = "0.1.0"
description = "Background worker shell for JobPilot local tasks."
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]

